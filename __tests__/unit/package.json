{
  "name": "unit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run --coverage",
    "watch": "DEBUG=1 vitest"
  },
  "devDependencies": {
    "vitepress-theme-mild": "workspace:*"
  }
}
